<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarPopupTheme">@style/AppTheme.PopupOverlay</item>
    </style>

    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabGravity">fill</item>
        <item name="tabMode">fixed</item>
        <item name="tabBackground">@drawable/tab_bg_selector</item>
        <item name="tabIconTint">@drawable/tab_item_selector</item>
        <item name="tabIndicatorHeight">0dp</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="iconTint">@color/colorWhite</item>
        <item name="titleTextColor">@color/colorWhite</item>
        <item name="colorControlNormal">@color/colorWhite</item>
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <item name="popupTheme">@style/OverflowPopupStyle</item>
    </style>
    <style name="OverflowPopupStyle" parent="Widget.AppCompat.Light.PopupMenu">
        <item name="android:colorBackground">@color/colorWhiteTrans</item>
        <item name="android:textColor">@color/colorBlack</item>
    </style>
    <style name="OverflowMenuStyle" parent="@style/Widget.AppCompat.PopupMenu">
        <item name="android:dropDownHorizontalOffset">@dimen/overflowHorOffset</item>
        <item name="android:dropDownVerticalOffset">@dimen/overflowVerOffset</item>
    </style>
</resources>
