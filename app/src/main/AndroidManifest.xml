<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kwang0.hackinssa">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.ui.activities.taginfo.TagInfoActivity"/>
        <activity android:name=".presentation.ui.activities.countryselect.CountrySelectActivity" />
        <activity android:name=".presentation.ui.activities.friendadd.FriendAddActivity" />
        <activity android:name=".presentation.ui.activities.countryinfo.CountryInfoActivity" />
        <activity android:name=".presentation.ui.activities.friendinfo.FriendInfoActivity" />
        <activity android:name=".presentation.ui.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>